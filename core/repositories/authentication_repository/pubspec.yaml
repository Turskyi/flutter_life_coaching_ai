name: authentication_repository
description: Dart package which manages the authentication domain.
publish_to: none
version: 1.0.0
repository: https://github.com/Turskyi/flutter_life_coaching_ai

environment:
  sdk: '>=3.9.2 <4.0.0'

dependencies:
  clerk_auth: ^0.0.13-beta
  envied: ^1.3.2
  models:
    path: ../../models
  shared_preferences: ^2.5.3

dev_dependencies:
  build_runner: ^2.10.4
  envied_generator: ^1.3.2
  lints: ^6.0.0
  test: ^1.28.0
